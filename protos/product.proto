syntax = "proto3";

package product;

message Empty {}
message ProductSnResponse {
    int32 sn = 1;
}
message ProductPriceResponse {
    int32 price = 1;
}

service Product {
    rpc GetPrice (Empty) returns (ProductPriceResponse);
    rpc GetSN (Empty) returns (ProductSnResponse);
}